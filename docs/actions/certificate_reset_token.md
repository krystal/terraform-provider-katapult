---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "katapult_certificate_reset_token Action - terraform-provider-katapult"
subcategory: "Networking"
description: |-
  Rotates the bearer token embedded in a certificate's certificate_api_url, invalidating the previous URL.
  The owning certificate resource's certificate_api_url attribute refreshes on the next plan, not within the apply that ran the action.
---

# katapult_certificate_reset_token (Action)

Rotates the bearer token embedded in a certificate's `certificate_api_url`, invalidating the previous URL.

The owning certificate resource's `certificate_api_url` attribute refreshes on the next plan, not within the apply that ran the action.

## Example Usage

```terraform
resource "katapult_self_signed_certificate" "internal" {
  name = "app.internal.example.com"
}

action "katapult_certificate_reset_token" "internal" {
  config {
    certificate_id = katapult_self_signed_certificate.internal.id
  }
}

# Rotate the bearer token embedded in `certificate_api_url` whenever this
# trigger resource is created or replaced. Change `input` to rotate again. The
# certificate resource's `certificate_api_url` refreshes on the next plan.
resource "terraform_data" "reset_token" {
  input = "2026-09-04"

  lifecycle {
    action_trigger {
      events  = [after_create, after_update]
      actions = [action.katapult_certificate_reset_token.internal]
    }
  }
}
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Required

- `certificate_id` (String) The ID of the certificate whose API token to reset.
